package com.theagilemonkeys.llmental.core

/**
 * This annotation is used to mark a parameter as mandatory,
 * so the compiler can check it at compile time.
 *
 *  Used by `PrimitiveModule` class to provide a better API by
 *  the modules that extend it.
 */
annotation class Mandatory
